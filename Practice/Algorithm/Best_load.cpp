#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

void loading(vector<int> &weight)
{
    sort(weight.begin(), weight.end());

    int sum_weight = 0;
    int count = 0;

    for (int i = 0; i < weight.size();i++)
    {
        if()
    }
}

int main()
{
    cout << "success" << endl;
    return 0;
}
