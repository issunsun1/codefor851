#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;
vector<int> weight;
vector<int> value;
int bagsize;

void resolution()
{
    vector< vector<int> > a(weight.size(), vector<int>(bagsize + 1, 0));
    int m = a.size();
    int n = a[0].size();

    
}

int main()
{
    return 0;
}